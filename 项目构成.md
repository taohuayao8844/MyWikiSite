# 项目构成

> ZeroPointNine 个人博客与知识整理站点 —— 项目结构与模块说明
> 框架:Docusaurus 2.2.x · 部署:GitHub Pages · 维护日期:2026-06-23

---

## 1. 项目简介

`ZeroPointNine 站`(原 `taohuayao8844.github.io`)是一个基于 **Docusaurus 2** 的个人博客 + 知识管理站点,主要承载三类内容:

- **Wiki / Docs** —— 大学课程、技术学习、嵌入式、AI 使用经验等系统化整理(放在 `/docs`)
- **Blog** —— 随笔、感想、阶段总结(放在 `/blog`)
- **Navigation** —— 收藏的常用网站、B 站 UP 主、友链、文件库(放在 `/bookmarks`、`/files`)

站点已对接 Giscus 评论、Umami/自建统计、KaTeX 公式、本地全文搜索。

---

## 2. 技术栈

| 类别 | 技术 |
|---|---|
| 静态站点框架 | Docusaurus 2.2 + `@docusaurus/preset-classic` |
| 本地全文搜索 | `@easyops-cn/docusaurus-search-local` |
| 数学公式 | KaTeX + `remark-math` / `rehype-katex` |
| 图标库 | `react-icons`(FA / SI 两套) |
| 视觉动效 | `react-tilt` 风格自实现 + `react-scroll-parallax` + 自研 CSS 动画 |
| 站点统计生成 | Node 脚本(`scripts/generate-site-statistics.js`) |
| 部署 | GitHub Pages(`taohuayao8844/taohuayao8844.github.io`) |
| 包管理 | npm / yarn / pnpm 三套 lock 文件并存 |

---

## 3. 顶层目录结构

```
BlogBok/
├── docusaurus.config.js     # 站点主配置(navbar / footer / 插件 / 主题)
├── sidebars.js              # docs 侧边栏配置
├── package.json             # 依赖与脚本
├── babel.config.js          # Babel 配置
├── README.md / LICENSE.md / notes.txt
│
├── docs/                    # Wiki / 文档源文件(Markdown)
│   ├── A_写在前面.md        # 序章
│   ├── A_工科入门基础篇/
│   ├── B_软件学习篇/        # Git、VSCode、Markdown…
│   ├── B_硬件学习篇/        # 嵌入式、PCB…
│   ├── B_找工实习篇/
│   ├── B_AI使用篇/
│   ├── C_编程语言了解篇/
│   ├── C_电赛/
│   ├── C_我在用什么系列/
│   ├── C_有感而发系列/
│   ├── C_浙江理工大学/
│   ├── D_杂记随笔篇/
│   └── Z_站点维护篇/        # 站点自身维护文档
│
├── blog/                    # 博客源文件
│   ├── _drafts/             # 草稿(Docusaurus 不会发布)
│   └── *.md                 # 已发布文章
│
├── src/                     # React 组件 / 页面 / 主题覆盖 / 静态资源
│   ├── components/          # 自研 React 组件
│   ├── css/                 # 全局自定义样式
│   ├── data/                # 数据文件(UP 主、友链、站点统计)
│   ├── pages/               # 自定义页面(bookmarks / files / index)
│   └── theme/               # Docusaurus 主题组件覆盖(swizzle)
│
├── static/                  # 构建期原样拷贝的静态资源
│   ├── files/               # 文件库 PDF(硬件手册 / 校园资料)
│   ├── fonts/               # 霞鹜文楷字体
│   ├── image_photo/         # 文章插图
│   ├── images/              # 通用图片 + 统计脚本
│   ├── img/                 # Logo / favicon
│   ├── person/              # 静态资源(被 `person/favicon.ico` 引用)
│   ├── .nojekyll
│   └── robots.txt
│
├── person/                  # 头像 / 背景图(被代码以 `person/xxx` 路径引用)
├── scripts/                 # 站点统计生成脚本
├── build/                   # docusaurus build 输出(已 gitignored)
├── .docusaurus/             # Docusaurus 缓存
└── node_modules/            # 依赖(已 gitignored)
```

---

## 4. `src/` 内部分层

### 4.1 `src/components/` — 自研 React 组件

| 组件 | 用途 |
|---|---|
| `MouseParticleEffect.js` | 全局鼠标跟随粒子动效(`/`) |
| `MenuHoverEffect.js` | 顶部导航菜单 hover 动效,通过 `Root.js` 全局挂载 |
| `TypewriterText.js` | 打字机切换文本 |
| `TiltCard.js` | 3D 倾斜卡片(首页用) |
| `GridBackground.js` | 网格背景动效 |
| `FocusHighlight.js` | 文字聚焦高亮 |
| `highlight.js` | 代码块关键词高亮辅助 |
| `tooltip.js` | 自定义 tooltip |
| `redirect.js` | 客户端重定向辅助 |
| `HomepageFeatures.js` | 旧版首页特性区 |
| **`HorizontalCard.js`** | **横向卡片,B 站 UP 主 / 友链共用布局**(本次新增) |

### 4.2 `src/pages/` — 自定义页面(路由)

| 路径 | 文件 | 说明 |
|---|---|---|
| `/` | `index.js` | 首页:身份区 + 关于我 + 个人信息 + 站点统计 |
| `/bookmarks` | `bookmarks.js` | **网站导航**:左侧栏(搜索 + 分类)+ 右侧(网站卡片网格 / 横向卡片列表) |
| `/files` | `files.js` | 文件库:PDF / 资料下载,按分类 + 文件夹 + 文件列表组织 |
| `/markdown-page` | `markdown-page.md` | 示例 MDX 页面 |

每个页面搭配同名 `*.module.css`(CSS Modules 作用域)。

### 4.3 `src/data/` — 数据文件

| 文件 | 内容 |
|---|---|
| `site-statistics.json` | 站点统计(文章数 / 运行时长 / 总字数 / 最近更新),由脚本生成 |
| **`upMasters.js`** | **B 站 UP 主数据**(本次新增) |
| **`friends.js`** | **友链数据**(本次新增,自原 `friends.js` 页面迁移) |

### 4.4 `src/css/` — 全局样式

- `custom.css` —— Docusaurus 全局样式覆盖(50 KB,涵盖深色/浅色主题、滚动条、字体、链接、菜单、表格等所有全局微调)
- `components/tootip.module.css` —— 旧版 tooltip 组件样式

### 4.5 `src/theme/` — 主题覆盖(swizzle)

通过 Docusaurus 的 `swizzle` 机制,在不修改原始包的前提下覆盖官方主题组件:

| 文件 | 覆盖官方组件 | 用途 |
|---|---|---|
| `Root.js` | `@theme/Root` | 注入全局 `MenuHoverEffect` |
| `DocItem/index.js` | `@theme/DocItem` | 文档页个性化 |
| `DocSidebar/index.js` | `@theme/DocSidebar` | 文档侧边栏 |
| `SearchPage/index.js` | `@theme/SearchPage` | 本地搜索结果页(通过 webpack alias 接入) |

### 4.6 `src/components/HorizontalCard.js`(本次新增)

横向卡片组件,**B 站 UP 主 + 友链共用**同一布局,通过 `variant` 切换头像来源:

- `variant="bili"` —— 从 B 站 UID 解析头像,头像右下角显示 B 站蓝 logo
- `variant="friend"` —— 从 URL favicon 服务解析,头像右下角显示「友」紫蓝渐变小角标

头像失败兜底:用 name 的 hash 计算稳定渐变色 + 首字母占位。

---

## 5. 路由总览

```
/                          → 首页 (src/pages/index.js)
/blog                      → Docusaurus Blog(/blog/*.md)
/A_写在前面的              → docs 序章
/docs/*                    → Wiki 文档(由 sidebars.js 控制侧边栏)
/bookmarks                 → 网站导航(搜索 + 分类 + 卡片)
/files                     → 文件库(PDF 资料下载)
```

> 原 `/friends` 已合并到 `/bookmarks` 的「友链」分类。

顶部 navbar:📗 Wiki · 📝 博客 · 📑 导航 · 📁 文件 · 🔍 搜索(⌘K)

底部 footer:Docs(导航入口) · Contact(GitHub / 微博 / Bilibili) · 备案号

---

## 6. 关键脚本

### `scripts/generate-site-statistics.js`

统计 `docs/` + `blog/` 下所有 `.md` 文件,输出文章数、总字数、最近更新日期,写入 `src/data/site-statistics.json`。

**由 `npm run start` / `npm run build` 自动调用**(在 `package.json` 的 scripts 里串联)。

```json
{
  "articleCount": 59,
  "runtime": "253 days",
  "totalCount": "116.9k",
  "lastUpdate": "Today"
}
```

---

## 7. 常用命令

```bash
npm run start          # 开发模式 + 自动生成统计
npm run build          # 生产构建 + 自动生成统计
npm run serve          # 本地预览 build 产物
npm run clear          # 清理 docusaurus 缓存
npm run swizzle        # swizzle 主题组件
npm run deploy         # 部署到 GitHub Pages
npm run generate:stats # 单独重新生成站点统计
```

---

## 8. 内容维护指南

### 新增书签 / 修改分类
编辑 `src/pages/bookmarks.js` 中的 `bookmarksData` 数组,每条记录字段:

```js
{
  name: '网站名',
  description: '一句话描述',
  url: 'https://...',
  category: '推荐 / 技术 / 大学 / 学术 / AI',
  icon: 'robot',          // 见 IconComponent 映射
  isPinned: false,
  isStarred: false,
}
```

### 新增 B 站 UP 主
编辑 `src/data/upMasters.js`:

```js
{
  name: '稚晖君',
  uid: '20259914',
  description: '描述...\n支持换行',
  tags: ['硬核', '机器人'],
  isPinned: true,
  isStarred: true,
}
```

### 新增友链
编辑 `src/data/friends.js`:

```js
{
  name: '博客名',
  url: 'https://...',
  description: '推荐理由...\n支持换行',
  tags: ['领域', '风格'],
  isFeatured: true,    // 强推(放最前)
  isPinned: true,
  isStarred: true,
}
```

### 新增文件库条目
1. 把 PDF 放进 `static/files/<分类>/`
2. 编辑 `src/pages/files.js` 中的 `fileLibraryData`

### 新增博客文章
在 `blog/` 下创建 `<slug>.md`,带 front matter(`title` / `slug` / `authors` / `tags`)。草稿放进 `blog/_drafts/`。

### 新增 Wiki 文档
在 `docs/<对应分类>/` 下创建 `.md`,然后在 `sidebars.js` 调整侧边栏。

---

## 9. 部署流程

1. 本地编辑 / 添加内容
2. `npm run build` 验证无报错
3. `git add . && git commit -m "..."`
4. `git push origin main` —— GitHub Actions 或 `npm run deploy` 推送到 `gh-pages` 分支
5. 站点通过 `https://taohuayao8844.github.io` 访问

---

## 10. 已知遗留事项

- `docs/` 下部分 Markdown 文件存在 broken internal link(`vscode 插件推荐.md`、`markdown.md`),需要补回对应文件或修正链接
- `bookmarks.module.css` 中有少量已 `display: none` 的遗留类(`bookmarkDescription` / `bookmarkCategory`),不影响视觉,可后续清理
- 三套 lock 文件(`package-lock.json` / `yarn.lock` / `pnpm-lock.yaml`)并存,建议选定一套并删除其余两个