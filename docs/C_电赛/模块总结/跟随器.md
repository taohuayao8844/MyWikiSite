# 跟随器的设计

## 什么是电压跟随器

电压跟随器（Voltage Follower），又称**缓冲放大器（Buffer Amplifier）**，是运算放大器最基本的应用电路之一。其核心特点和应用价值如下：

## 基本原理

电压跟随器的电路结构非常简单：将运算放大器的输出端直接连接到反相输入端（如示波器探头所示）。根据运放的"虚短"特性，可得 **Uout ≈ Uin**，电压增益 **Av = 1** （但是我们一般不使用这样的电路，而是使用正向反向比例放大电路来实现跟随器，这样设计电路容易导致自激--不焊接任何电阻）

![image-20260509111732388](/image_photo/diansai/image-20260509111732388.png)

经常使用的两个电路：

![image-20260509111939199](/image_photo/diansai/image-20260509111939199.png)

![image-20260509111953027](/image_photo/diansai/image-20260509111953027.png)

## 主要作用

### 隔离缓冲（阻抗匹配）

这是电压跟随器最核心的价值所在：

- **高输入阻抗**：借助运算放大器极高的输入阻抗，不消耗前级信号源的电流（）
- **低输出阻抗**：通过极低的输出阻抗驱动后级低阻抗负载

这种特性使其能够**有效地隔离前后级电路**，防止高阻抗信号源被低阻抗负载过度抽取电流，从而保护信号源并提供稳定的信号传输。

### 阻抗变换

在电子系统中，前级电路的输出阻抗往往较高，而后级负载的输入阻抗较低。电压跟随器可以实现阻抗匹配，使得信号能够高效地从高阻抗源传递到低阻抗负载，同时保持信号幅度不变。

### 信号缓冲与分配

- 可以将一个信号源同时分配给多个负载
- 避免多负载直接连接导致的信号衰减
- 提供干净的信号副本

### 简单的信号调理

虽然电压增益为1，但在实际应用中，电压跟随器可以配合其他电路实现更复杂的功能，如电平转换、直流偏置调整等。

## PCB设计及电阻焊接选择

根据电路，我设计了一版通用的电路--V1

![image-20260509112315684](/image_photo/diansai/image-20260509112315684.png)

一般理想的跟随都是一倍放大，那么

在反向比例放大电路中是比较好实现的，只要选取`R负`和`Rf`电阻相等即可实现跟随器的设计。

1. 设计的时候在电源的正负5V都添加了去耦电容，能够有效的实现莫名奇妙产生的自激，一般选择100nf和1uf
2. 在输出有添加一个R输出作为输出的限流电阻，这边阻值选取也是有一定的说法

## 前置知识

[朋友，你听说过电流反馈型运放吗？ - 知乎](https://zhuanlan.zhihu.com/p/374633917)

电压反馈型运放具有**增益带宽积**，而电流反馈型则没有这个概念；电流反馈型运放具有**更高的带宽和输出压摆率**；

电压型反馈运放

![image-20260509123046420](/image_photo/diansai/image-20260509123046420.png)

![image-20260509123217965](/image_photo/diansai/image-20260509123217965.png)

![image-20260509114939080](/image_photo/diansai/image-20260509114939080.png)

## 具体选型设计

>经验总结
>
>实验室在高频跟随主要使用opa690和opa695两块芯片，有时会选取opa847这块芯片，由于芯片的特性不同其电路设计又有不一样的注意。

### OPA695--[OPA695手册](https://www.ti.com.cn/cn/lit/ds/symlink/opa695.pdf?ts=1778237815534&ref_url=https%3A%2F%2Fwww.ti.com.cn%2Fproduct%2Fcn%2FOPA695)--电流反馈

![image-20260509113059899](/image_photo/diansai/image-20260509113059899.png)

这是一块电流反馈运算放大器，有着很高的增益带宽。

>### 电流反馈运算放大器（CFA）与电压反馈运算放大器（VFA）的区别?
>
>**电流反馈型运放**没有输入平衡，其**同相输入端为高阻抗(可以很好的抓取前级信号)，反相输入端为低阻抗**；
>
>电压反馈型和电流反馈型放大器的另一个差异,就是**电流反馈型放大器的反相输入阻抗较低(通常为50 Ω至100 Ω)，而同相输入阻抗则较高(通常为几百kΩ)**。因此，电流反馈型放大器的输入不平衡，而电压反馈型放大器的输入平衡；
>电流反馈型运放的开环增益用Ω为单位来衡量（跨导增益），而不是电压反馈型运放用V/V表示；
>反馈电阻R2为固定值，电路反馈型运放闭环增益可通过调整R1而发生变化，不会对闭环带宽产生重大影响；通过检查图2中的简化公式就可以发现这一点。分母决定整体频率响应，如果R2不变，则可以改变分子中的R1(从而改变增益)，不影响分母，这样带宽就会保持相对稳定；
>电流反馈型和电压反馈型放大器的一个主要差异就是电流反馈型放大器没有恒定的增益带宽；电流反馈型运算放大器的带宽与反馈电阻成比例。每个电流反馈型运算放大器都有最大带宽时的推荐反馈电阻值。如果电阻值增加后超过该推荐值，就要减小带宽。
>如果采用阻值低于推荐值的电阻，相位裕量就会减小，放大器可能会变得不稳定；
>控制电流反馈型运算放大器应用中的增益时，需要为器件选择正确的反馈电阻(R2)，然后选择底部电阻(R1)，以产生所需的闭环增益；
>
>![image-20260509113759933](/image_photo/diansai/image-20260509113759933.png)电流反馈型放大器的反馈环路中也不应该有电容。如果反馈环路中采用电容，就会减小高频净反馈阻抗，导致运算放大器发生振荡。反相输入上的杂散电容会产生类似的效应，应去除反相端四周的接地层，尽量减小杂散电容；
>使用电流反馈运算放大器时常见错误是把反相输入直接连接到输出，试图构建单位增益电压跟随器(缓冲)。该电路会产生振荡，因为等效反馈电阻值为零；
>电流反馈放大器没有理论压摆率限制；

[电流反馈型运放与电压反馈型运放比较_电流型运放和电压型运放的区别-CSDN博客](https://blog.csdn.net/mogutou520/article/details/139466005)

### OPA690--[OPA690手册](https://www.ti.com.cn/cn/lit/ds/symlink/opa690.pdf?ts=1778298153387&ref_url=https%3A%2F%2Fwww.ti.com.cn%2Fproduct%2Fcn%2FOPA690)--电压反馈

opa690是电压型反馈放大运放，其芯片手册参数如下

![image-20260509114243593](/image_photo/diansai/image-20260509114243593.png)

这边的参数就是增益带宽积了

![image-20260509123300289](/image_photo/diansai/image-20260509123300289.png)

## 电流反馈运算放大器（CFA）与电压反馈运算放大器（VFA）应用

### 电压反馈运放（VFA）更适合：

- 精密仪器仪表（万用表、传感器信号调理）
- 有源滤波器设计
- 积分器、微分器等精确模拟计算
- 需要低噪声、低失真的音频处理
- 直流-coupled精密放大

### 电流反馈运放（CFA）更适合：

- **高速ADC/DAC驱动**
- 视频信号处理（75Ω驱动能力）
- 宽带通信前端
- 脉冲放大器
- 需要高slew rate的快速波形生成

## 电流型运放使用注意事项

（一）禁止作为积分器使用

CFB 运放的输入结构和反馈机制决定了其不适合积分运算场景。积分电路要求输入电流与时间成积分关系，而 CFB 运放的误差电流受输入缓冲器动态特性影响，难以满足积分精度要求。

（二）反馈电阻两端的电容规避

在 CFB 运放的 Rf 两端并联电容可能破坏其反馈稳定性，引发振荡。因电容会在高频段形成低阻抗通路，干扰反馈电流路径，破坏相位裕度。

（三）输出与反相输入端跨接电容的限制

跨接电容会引入额外相位滞后，削弱 CFB 运放的相位裕度，特别是在高频工作状态下，可能导致电路振荡。

（四）反馈误差与输出关系

CFB 运放的反馈误差体现为运放反相输入端的电流值，输出电压可表示为 Vout=Zf×Iin。因此，Zf 的选取需综合考虑信号频率、负载特性等因素。

（五）反馈电阻的合理取值

CFB 运放的 Rf 既影响增益精度，又关联稳定性与带宽。过大的 Rf 可能因寄生电容效应降低 slewrate，过小的 Rf 则可能因反馈电流过大导致运放饱和。存在一个最佳 Rf 值，既能实现最大带宽，又能保障稳定工作。

（六）禁止电压跟随器接法

CFB 运放的输入结构决定了其不适合作为电压跟随器。电压跟随器要求高输入阻抗、低输出阻抗以及单位增益稳定，而 CFB 运放的输入缓冲器输出阻抗相对较高，难以满足该要求，易导致电路不稳定。

（七）压摆率优势

CFB 运放凭借其高 slewrate，在快速信号跃变场景中表现出色，能迅速响应输入信号变化，维持输出信号的保真度。

（八）无增益带宽积限制

CFB 运放突破了 GBW 束缚，增益与带宽的独立调节特性使其在宽带信号放大应用中更具灵活性。

（九）增益与闭环带宽的独立设置

CFB 运放允许设计人员依据具体需求分别设定增益与闭环带宽，无需在二者之间进行折中权衡。

（十）同向与反相输入计算公式

CFB 运放的同向与反相输入计算公式与电压反馈型运放基本一致，方便设计人员快速上手。